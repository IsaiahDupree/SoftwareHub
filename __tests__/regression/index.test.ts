/**
 * Regression test index - ensures all regression tests are discoverable
 */
describe("Regression tests", () => {
  it("regression test directory is accessible", () => {
    expect(true).toBe(true);
  });
});
